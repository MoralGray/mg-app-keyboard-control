{
    "name": "mg-app-keyboard-control",
    "version": "0.0.1",
    "type": "module",
    "private": false,
    "exports": {
        ".": {
            "types": "./src/index.ts",
            "default": "./src/index.ts"
        }
    },
    "scripts": {
        "release": "bun scripts/build-all.ts",
        "clean": "rimraf ./dist && rimraf ./bin",
        "format": "npx biome format --write .",
        "lint": "npx biome lint --write .",
        "prerelease": "npx biome check --write . && npx tsc"
    },
    "devDependencies": {
        "@biomejs/biome": "2.3.11",
        "@types/node": "22.10.2",
        "@types/bun": "1.3.8",
        "rimraf": "6.0.1",
        "typescript": "5.7.3",
        "vite": "^8.0.14"
    },
    "engines": {
        "bun": ">=1.0.0",
        "node": ">=18.0.0"
    }
}
