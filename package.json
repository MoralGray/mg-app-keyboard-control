{
    "name": "mg-app-keyboard-control",
    "version": "0.0.1",
    "type": "module",
    "private": false,
    "exports": {
        ".": {
            "types": "./src/index.ts",
            "default": "./src/index.ts"
        }
    },
    "scripts": {
        "release": "bun scripts/build-all.ts",
        "clean": "rimraf ./dist && rimraf ./bin",
        "format": "biome format --write .",
        "lint": "biome lint --write .",
        "test": "vitest run",
        "test:watch": "vitest",
        "prerelease": "biome check --write . && tsc"
    },
    "devDependencies": {
        "@biomejs/biome": "2.3.11",
        "@types/bun": "1.3.8",
        "@types/node": "^22.14.1",
        "jsdom": "^27.0.1",
        "rimraf": "6.0.1",
        "typescript": "5.7.3",
        "vite": "^8.0.14",
        "vitest": "^3.2.6"
    },
    "engines": {
        "bun": ">=1.0.0",
        "node": ">=18.0.0"
    }
}
